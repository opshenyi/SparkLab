# Database
DATABASE_URL="file:./prisma/spark_lab.db"

# Server Configuration
PORT=3001
HOST=localhost

# Docker Engine (local daemon)
DOCKER_HOST=unix:///var/run/docker.sock

# GitHub Updates
GITHUB_REPO=opshenyi/SparkLab
GITHUB_BRANCH=main
UPDATE_CHECK_CACHE_SECONDS=300
UPDATE_SCRIPT_TIMEOUT_SECONDS=600
# Optional: increases GitHub API rate limit for update checks
# GITHUB_TOKEN=
# Optional: set when the server is not started from inside the repository
# APP_REPO_DIR=
# Optional: command executed after code is pulled and build succeeds
# SPARKLAB_RESTART_COMMAND=

# JWT Secret (change this in production!)
JWT_SECRET=your-secret-key-change-this-in-production

# CORS
CORS_ORIGIN=http://localhost:3000

# Environment
NODE_ENV=development
